<%= form_with(model: pet, local: true) do |form| %>
  <% if pet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pet.errors.count, "error") %> prohibited this pet from being saved:</h2>

      <ul>
      <% pet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :pet_name %>
  </div>

  <div class="field">
    <%= form.label :species %>
    <%= form.text_field :species, id: :pet_species %>
  </div>

  <div class="field">
    <%= form.label :color %>
    <%= form.text_field :color, id: :pet_color %>
  </div>

  <div class="field">
    <%= form.label :image_url %>
    <%= form.text_field :image_url, id: :pet_image_url %>
  </div>

  <div class="field">
    <%= form.label :age_in_months %>
    <%= form.number_field :age_in_months, id: :pet_age_in_months %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :pet_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
